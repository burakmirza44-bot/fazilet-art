[phases.setup]
nixPkgs = ["nodejs_22", "python3", "pkg-config", "gcc"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production npx tsx server.ts"
